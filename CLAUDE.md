# Standing instructions

- **Always push to `main`** after making and committing a change. The user
  (chris209code) has given this instruction repeatedly (most recently
  confirmed 2026-08-24) — do not park work on a feature/topic branch and
  wait for a merge unless the user explicitly asks for a PR/branch workflow
  for that specific piece of work. Default to pushing straight to `main`.
